<IfModule mod_rewrite.c>
    RewriteEngine On
    # Use `public/` as the real web root. If the server points at the repo root, forward into public/.
    RewriteCond %{REQUEST_URI} !/public/ [NC]
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
