<IfModule mod_rewrite.c>
    RewriteEngine On

    # Serve existing files/directories (assets, etc.) as-is
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    RewriteRule ^ index.php [L,QSA]
</IfModule>
