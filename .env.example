# Copy to .env and adjust (never commit .env)

# MySQL
BAG_DB_HOST=localhost
BAG_DB_USER=root
BAG_DB_PASS=
BAG_DB_NAME=bag_biz

# Admin login username (must match a row in users.username)
BAG_ADMIN_USER=admin
